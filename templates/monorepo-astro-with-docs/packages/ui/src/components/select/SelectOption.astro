---
import type { HTMLAttributes } from "astro/types";
import { cn } from "@repo/ui/lib/utils";

type Props = {
  value?: string;
  disabled?: boolean;
  selected?: boolean;
  class?: string;
} & HTMLAttributes<"option">;

const {
  value,
  disabled = false,
  selected = false,
  class: className = "",
  ...rest
} = Astro.props as Props;
---

<option
  {...rest}
  data-slot="select-option"
  value={value}
  disabled={disabled}
  selected={selected}
  class={cn(
    "checked:text-primary/50 flex items-center gap-2 outline-none",
    "px-2 py-1.5 text-sm [&:hover]:bg-accent [&:hover]:text-accent-foreground",
    "rounded-sm cursor-default transition-discrete select-none",
    className,
  )}
>
  <slot />
</option>
