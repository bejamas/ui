---
import { cn } from "@bejamas/registry/lib/utils";
import type { HTMLAttributes } from "astro/types";

interface Props extends HTMLAttributes<"button"> {
  class?: string;
  /** Radix-like: render wrapper so the child can be the interactive element */
  asChild?: boolean;
}

const {
  class: className = "",
  asChild = false,
  type = "button",
  ...rest
} = Astro.props as Props;
---

{
  asChild ? (
    <div data-slot="collapsible-trigger" data-as-child class={cn("cn-collapsible-trigger", className)}>
      <slot />
    </div>
  ) : (
    <button
      {...rest}
      type={type}
      data-slot="collapsible-trigger"
      class={cn("cn-collapsible-trigger", className)}
    >
      <slot />
    </button>
  )
}
