---
import type { HTMLAttributes } from "astro/types";
import { cn } from "@bejamas/registry/lib/utils";
import SemanticIcon from "../icon/SemanticIcon.astro";

interface Props extends HTMLAttributes<"div"> {
  disabled?: boolean;
  inset?: boolean;
  value: string;
  defaultChecked?: boolean;
  class?: string;
}

const {
  disabled = false,
  inset = false,
  value,
  defaultChecked = false,
  class: className = "",
  ...rest
} = Astro.props as Props;
---

<div
  {...rest}
  role="menuitemcheckbox"
  tabindex={disabled ? -1 : 0}
  data-slot="dropdown-menu-checkbox-item"
  data-value={value}
  data-disabled={disabled ? "" : undefined}
  data-inset={inset ? "" : undefined}
  data-default-checked={defaultChecked ? "" : undefined}
  class={cn(
    "cn-dropdown-menu-checkbox-item",
    "group/dropdown-menu-item relative flex cursor-default items-center outline-hidden select-none data-[disabled]:pointer-events-none data-[disabled]:opacity-50 [&_svg]:pointer-events-none [&_svg]:shrink-0",
    className,
  )}
>
  <slot />
  <span
    data-slot="dropdown-menu-item-indicator"
    class="cn-dropdown-menu-item-indicator text-current opacity-0 transition-opacity group-data-checked/dropdown-menu-item:opacity-100"
    aria-hidden="true"
  >
    <SemanticIcon name="check" class="size-4" />
  </span>
</div>
