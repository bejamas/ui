---
import { SearchIcon } from "@lucide/astro";
import type { HTMLAttributes } from "astro/types";
import { cn } from "@bejamas/registry/lib/utils";
import { InputGroup, InputGroupAddon } from "../input-group";
import { inputGroupControlClasses } from "../input-group/controlClasses";

interface Props extends HTMLAttributes<"input"> {
  class?: string;
}

const { class: className = "", ...rest } = Astro.props as Props;
---

<div data-slot="command-input-wrapper" class="cn-command-input-wrapper">
  <InputGroup size="sm" class="cn-command-input-group">
    <input
      data-input-group-control
      data-slot="command-input"
      autocomplete="off"
      autocorrect="off"
      spellcheck="false"
      class={cn(
        "cn-input-group-control cn-input-group-input cn-command-input w-full text-sm outline-hidden disabled:cursor-not-allowed disabled:opacity-50",
        inputGroupControlClasses,
        className,
      )}
      {...rest}
    />
    <InputGroupAddon align="inline-end">
      <SearchIcon
        data-slot="command-input-icon"
        class="cn-command-input-icon pointer-events-none"
      />
    </InputGroupAddon>
  </InputGroup>
</div>
