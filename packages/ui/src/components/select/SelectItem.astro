---
import type { HTMLAttributes } from "astro/types";
import { cn } from "@bejamas/ui/lib/utils";
import CheckIcon from "@lucide/astro/icons/check";

interface Props extends HTMLAttributes<"div"> {
  value: string;
  disabled?: boolean;
  class?: string;
}

const {
  value,
  disabled = false,
  class: className = "",
  ...rest
} = Astro.props as Props;
---

<div
  {...rest}
  role="option"
  tabindex={disabled ? -1 : 0}
  data-slot="select-item"
  data-value={value}
  data-disabled={disabled ? "" : undefined}
  class={cn(
    "cn-select-item",
    "group/item relative border border-transparent flex w-full cursor-default select-none items-center gap-1.5 rounded-md pr-8 pl-2 text-sm outline-hidden",
    "group-data-[size=sm]/select-content:py-1 group-data-[size=default]/select-content:py-1.5 group-data-[size=lg]/select-content:py-2",
    "data-[highlighted]:bg-accent data-[highlighted]:text-accent-foreground",
    "data-[disabled]:pointer-events-none data-[disabled]:opacity-50",
    "[&_svg]:pointer-events-none [&_svg:not([class*='size-'])]:size-4 [&_svg]:shrink-0",
    className,
  )}
>
  <span
    data-slot="select-item-text"
    class="cn-select-item-text flex flex-1 gap-2 shrink-0 whitespace-nowrap items-center"
    ><slot /></span
  >

  <span
    data-slot="select-item-indicator"
    class="cn-select-item-indicator pointer-events-none absolute right-2 flex size-4 items-center justify-center"
  >
    <span class="hidden items-center justify-center group-data-[selected]/item:inline-flex">
      <CheckIcon class="size-4" />
    </span>
  </span>
</div>
