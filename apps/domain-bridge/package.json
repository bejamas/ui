{
  "name": "domain-bridge",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "wrangler deploy --env=\"\" --dry-run --outdir dist && wrangler deploy --env og --dry-run --outdir dist-og",
    "check-types": "wrangler types && tsc --noEmit",
    "deploy:web": "wrangler deploy --env=\"\"",
    "deploy:og": "wrangler deploy --env og",
    "test": "bun test"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
