---
import type { HTMLAttributes } from "astro/types";
import { cn } from "@bejamas/ui/lib/utils";
import SemanticIcon from "../icon/SemanticIcon.astro";

interface Props extends HTMLAttributes<"div"> {
  value: string;
  disabled?: boolean;
  class?: string;
}

const {
  value,
  disabled = false,
  class: className = "",
  ...rest
} = Astro.props as Props;
---

<div
  {...rest}
  role="option"
  tabindex={disabled ? -1 : 0}
  data-slot="select-item"
  data-value={value}
  data-disabled={disabled ? "" : undefined}
  class={cn(
    "cn-select-item",
    "group/item relative flex w-full cursor-default select-none items-center outline-hidden",
    "data-[disabled]:pointer-events-none data-[disabled]:opacity-50",
    "[&_svg]:pointer-events-none [&_svg]:shrink-0",
    className,
  )}
>
  <span
    data-slot="select-item-text"
    class="cn-select-item-text shrink-0 whitespace-nowrap"
    ><slot /></span
  >

  <span
    data-slot="select-item-indicator"
    class="cn-select-item-indicator pointer-events-none absolute right-2 flex size-4 items-center justify-center"
  >
    <span class="hidden items-center justify-center group-data-[selected]/item:inline-flex">
      <SemanticIcon name="check" class="size-4" />
    </span>
  </span>
</div>
