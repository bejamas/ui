---
"bejamas": patch
---

Fix published `bejamas init` project creation by falling back to starter templates downloaded from the `bejamas/ui` GitHub repo when local bundled templates are unavailable.
