---
"bejamas": minor
---

Add a Bejamas-native `apply` command for preset switching, including partial `--only theme`, `--only font`, and `--only theme,font` support, and bump the managed shadcn runner to 4.6.0.
